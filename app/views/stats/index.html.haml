- content_for :page_title_prefix, "Statistics"

.grid-row
  .column-two-thirds
    %h1.heading-xlarge
      Statistics

.grid-row
  - @audit_summary.each do |key, value|
    = render partial: 'stat', locals: { key: key, value: value }
